name: Run Workflow Every 5 Minutes

on:
  schedule:
    - cron: '*/5 * * * *'  # Schedule to run every 5 minutes

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2
    - name: Set up Node.js
      uses: actions/setup-node@v2
      with:
        node-version: '16'  # Specify Node.js version 16
    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: '3.x'
    - name: Install Dependencies
      run: |
        pip install requests
        pip install beautifulsoup4
        pip install twilio
    - name: Run Script
      run: python fetch_changes.py ${{ secrets.PHONE_NUMBER_1 }} ${{ secrets.PHONE_NUMBER_2 }} ${{ secrets.ACCOUNT_SID }} ${{ secrets.AUTH_TOKEN }}
